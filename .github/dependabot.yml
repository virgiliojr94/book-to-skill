version: 2
updates:
  # Keep GitHub Actions fresh (and enables pinning actions to SHAs later).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
